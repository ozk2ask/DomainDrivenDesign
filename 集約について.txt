オブジェクトの集約の境界内外でのアクセスは制限されるべきである。
集約ルートを介してのみ、境界内部のオブジェクトへアクセスできる。

ファクトリパターンによって、オブジェクトの生成と利用を明確に分けてカプセル化する。
イベントクラスについては、抽象とファクトリメソッドを使うことで、実装を知らずに利用することができる。

イベントに対して、プログラムではなくクエリで問い合わせを行う場合にリポジトリを作成する。
メモリ上で対応するか、クエリで対応するかは、性能面でのトレードオフが起きることもある。
仕様によって、なにが優先順位が高いかを検討して採用する必要がある。

